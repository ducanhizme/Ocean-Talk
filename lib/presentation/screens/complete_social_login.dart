import 'package:flutter/material.dart';

class CompleteSocialLoginScreen extends StatelessWidget {
  const CompleteSocialLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Complete Social Login Screen'),
      ),
    );
  }
}
